version: '3.8'

services:
  playwright-tests:
    build:
      context: .
      dockerfile: Dockerfile
    image: agibank-e2e-tests:latest
    container_name: agibank-e2e-tests
    volumes:
      - ./test-results:/app/test-results
      - ./playwright-report:/app/playwright-report
    environment:
      - CI=true
      - NODE_ENV=test
    networks:
      - test-network

networks:
  test-network:
    driver: bridge

